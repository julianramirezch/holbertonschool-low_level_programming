#include "holberton.h"
#include <stdio.h>
/**
 * _abs - computes the absolute value of an integer
 * @j: Integer
 * Return: Always 0.
 */
int _abs(int j)
{

if (j < 0)
{
	j = j * (-1);
	return (j);
}
else
{
return (j);
}
}
