#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - function tha returns the length of a string.
 * @s: Char
 * Return: Always 0.
 */

int _strlen(char *s)
{
int j;
for (j = 0; *(s + j) != '\0'; j++)
{
}
return (j);
}
