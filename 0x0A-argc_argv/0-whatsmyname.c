#include <stdio.h>

/**
 * main - Program that prints its name.
 * @argc: Int.
 * @argv: char
 * Return: 0
 */

int main(int argc  __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
return (0);
}
